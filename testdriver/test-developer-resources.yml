version: 4.0.78
steps:
  - prompt: Click on "Developers" in the header.
    commands:
      - command: hover-text
        text: Developers
        description: link in the header
        action: click
  - prompt: >-
      Assert that developer resources such as API documentation are accessible. 
      # Use the content of the file from the environment
    commands:
      - command: assert
        expect: Documentation is visible on the page
